module.exports = require('./defineStatement')('create', {

	init: function(what){
		this.callSuper()
		this.target = what
	},

	toString: function(){
		if (this.target && typeof this.target.createToString == 'function'){
			return this.target.createToString()
		}

		return 'CREATE ' + this.target
	}
})